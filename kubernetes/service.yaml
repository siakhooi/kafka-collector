apiVersion: v1
kind: Service
metadata:
  name: kafka-collector
  labels:
    app: kafka-collector
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: kafka-collector
